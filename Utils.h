#pragma once

//Header file for Utils, a file with useful functions to use throughout the program that really don't fit on any other file (to be implemented)

unsigned int readInteger(unsigned int minValue, unsigned int maxValue); //Reads integer value in range [minValue, maxValue]